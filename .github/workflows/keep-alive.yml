name: Keep Render Server Alive

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl -f https://timezone-solver-app.onrender.com/ || echo "Ping failed, continuing anyway"